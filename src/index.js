console.log("Hello world");
console.log(
  "This repository demonstrates an automated workflow using GitHub Actions to keep the develop branch in sync with the main branch. Whenever changes are pushed to the main branch (such as hotfixes or updates), the workflow will automatically merge these changes into the develop branch, ensuring both branches are always up-to-date."
);
